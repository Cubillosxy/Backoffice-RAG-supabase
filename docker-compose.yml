services:
  back:
    build:
      context: ./back
      dockerfile: Dockerfile
    container_name: backoffice_rag_backend
    ports:
      - "8000:8000"
    volumes:
      - ./back:/app
    env_file:
      - ./back/.env
    environment:
      - PYTHONUNBUFFERED=1
